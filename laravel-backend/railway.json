{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "php artisan migrate --force 2>&1 && echo 'Starting server on port ${PORT:-8000}...' && php artisan serve --host=0.0.0.0 --port=${PORT:-8000} 2>&1",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
